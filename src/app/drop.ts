export class Drop {
    quote: string;
    author: number;
    name: string;
    constructor(quote:string, author:number, name:string){
        this.quote = quote;
        this.author = author;
        this.name = name;
    }
}